dependencies:
  override:
    - npm install -g eslint
  cache_directories:
    - "node_modules"
deployment:
  production:
    branch: master
    commands:
      - git subtree push --prefix app origin gh-pages
